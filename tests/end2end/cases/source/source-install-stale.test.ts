import { describe, it } from 'vitest';

describe.skip('install with stale checkout', () => {
  it('is superseded by source-stale-checkout e2e coverage for the supported stale-checkout update flow', () => {});
});
