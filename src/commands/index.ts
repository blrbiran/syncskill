// Commands barrel export
// NOTE: Full command extraction from index.ts is pending.
// These exports provide the new modular structure.

export { runDashboard } from './dashboard.js';
export { runInit, type InitOptions } from './init.js';

// Future: registerAllCommands will be added when full extraction is complete
